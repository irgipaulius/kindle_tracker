{
  "name": "kindle-tracker",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix worker && npm install --prefix client && npm install --prefix server",
    "dev": "concurrently -n worker,client -c blue,green \"npm run dev:worker\" \"npm --prefix client run dev\"",
    "dev:worker": "wrangler dev",
    "dev:legacy": "concurrently -n server,client -c blue,green \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build:all": "npm --prefix client run build",
    "build": "npm run build:all",
    "build:deploy": "npm run build:all && wrangler deploy",
    "deploy": "npm run build:deploy",
    "typecheck:worker": "npm --prefix worker run typecheck",
    "serve:backend": "npm --prefix server run start",
    "serve:frontend": "npm --prefix client run preview"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "wrangler": "^3.94.0"
  }
}
